<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.example.dating.viewmodels.NationalitiesViewModel" />

        <variable
            name="item"
            type="com.example.dating.models.NationalityModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/largeMargin"
        android:orientation="horizontal">

        <androidx.appcompat.widget.AppCompatTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/normalMargin"
            android:drawableStart="@{item.isSelected ? @drawable/radio_selected : @drawable/radio_unselected}"
            android:drawablePadding="@dimen/normalPadding"
            android:onClick="@{(view) -> viewModel.nationalityItemClicked(view, item)}"
            android:padding="@{@dimen/normalPadding}"
            android:text="@{item.label}"
            android:textColor="@color/textColor"
            android:textSize="@dimen/largeTextSize" />
    </LinearLayout>
</layout>